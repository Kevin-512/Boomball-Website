"use client";
import Header from "./Header";

const Hero = () => {
  return (
    <section className="h-[800px] relative bg-primary/75 xl:bg-primary">
        <Header />
    </section>
  );
};

export default Hero;
